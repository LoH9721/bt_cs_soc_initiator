# CR009 手机 PEPS 自动解闭锁策略修改记录

更新时间：2026-08-19

当前状态：CR009-006 已实施（TESTED）；CR009-001 已实施（BUILT，实机待测）；CR009-005 已实施（BUILT，实机待测）；CR009-002/003/004 已取消（CANCELLED）。

本文件只记录已经实施的实际改动，不复制候选计划或实时状态。计划与边界见 `CR009_PHONE_PEPS_AUTO_LOCK_PLAN.md`，实时状态见 `CR_STATUS.md`。

---

## CR009-006 自动解锁额度语义

- 状态：TESTED（用户确认编译和实机测试通过）
- 原有逻辑：断连自动闭锁（CR008-010）的启动和取消条件额外挂了“自动解锁额度”判断，额度=0 时离车断连 5s 不再自动闭锁。
- 必要性：额度是“自动解锁”额度，不应卡自动闭锁。
- 不修改风险：不改则额度=0 时离车不自动闭锁。
- 实际改动：删除 `user_app_phone_peps.c` 断连闭锁启动条件的 `&& phone_comm_get_passive_quota() > 0U`；删除取消条件的 `phone_comm_get_passive_quota() == 0U ||`。
- 文件与函数：`user_app_fun/user_app_phone_peps.c` 的 `user_app_phone_peps_process()`。
- 影响范围：只影响断连自动闭锁门控；额度扣减逻辑和自动解锁路径不变。
- 与 CR008 和 CR009 其他子项的冲突检查：不改 CR008-010 授权断连事件来源；与 CR009-001 同段代码，已先做本项。
- 编译方法：Simplicity Studio 编译，仅改 `.c`。
- 前后对比实测方法：额度=0 断连 5s 仍闭锁；额度>0 行为不变；自动解锁仍扣额度；回归 CR008-010。
- 实际验证证据：用户确认编译和实机测试通过。
- 提交：待提交。

---

## CR009-001 车辆自动动作门控

- 状态：BUILT（用户确认编译通过，实机待测）
- 原有逻辑：自动解锁只看锁状态（`lock_state == UNLOCKED` 跳过），断连闭锁只看 `lock_state != LOCKED`，均不看静默、门/尾门。
- 必要性：静默期（连续 3 次安全失败锁定）仍可能自动解闭锁；门/尾门开着时断连闭锁可能夹人/夹物。
- 不修改风险：不改则静默期仍动作、门/尾门开仍可能断连闭锁。
- 实际改动：
  - 新增 `phone_sm_is_silent()` / `phone_comm_is_silent()` 访问器；
  - 尾门 `BackDoorSt` 并入 `door_status` 第 5 位（`0x10`）；
  - 自动解锁加静默拦截；
  - 断连闭锁启动加“非静默 + 门/尾门全关”，取消加“静默开始或门/尾门打开”。
- 文件与函数：`user_phone/data/phone_sm.c/.h`、`user_phone/phone_comm.c/.h`（静默访问器）；`user_app_fun/user_app_fun.c`（尾门并入 door_status）；`user_app_fun/user_app_phone_peps.c` 的 `user_app_phone_peps_process()`（门控）。
- 影响范围：只影响 PEPS 自动解锁/断连闭锁门控；不改 CR008 授权语义、冻结接口和串口。
- 与 CR008 和 CR009 其他子项的冲突检查：不改 CR008-009 授权门控；收紧 CR008-010 断连闭锁条件（需回归）；与 CR009-006 同段代码，006 已先提交；与 CR009-002/003 不冲突。
- 编译方法：Simplicity Studio 编译，仅改 `.c/.h`。
- 前后对比实测方法：静默期不自动解锁/闭锁；门/尾门开不闭锁；全关+断连 5s 正常闭锁；计时中开门取消；回归 CR008-009/010、CR009-006。
- 实际验证证据：用户确认编译通过，实机待测。
- 提交：待提交。

---

## CR009-002 Approach / Leave Cycle 与目标状态门闩

- 状态：CANCELLED（未实施即取消，代码已回退）
- 取消原因：自动解锁只在“从闭锁区（LOCK/INVALID）到解锁区/车内”的区域转换时触发；触发必经闭锁区确认，门闩在进入闭锁区时已复位，`g_unlock_spent` 在触发点恒为 false，属 no-op。防反复解锁由现有“已解锁跳过 + 迟滞 + 5 次去抖 + 2s 冷却”覆盖。
- 后续：闭锁门闩对断连闭锁的防重复增强（`g_lock_spent`）价值窄，留到 CR009-005 断连兜底按需设计。
- 提交：未提交（代码已回退）。

---

## CR009-003 Phone Auto 命令结果闭环

- 状态：CANCELLED（未实施即取消）
- 取消原因：二态锁状态 + one-shot 命令下作用有限——TARGET_REACHED 近乎 no-op（“已解锁跳过”已覆盖），OPPOSITE_STATE 不适用，初版不自动重试也解决不了断连闭锁失败风险；主要价值是日志诊断，不值得单独 CR。
- 后续：如需“命令是否生效”日志，作为 CR009-005 断连兜底的可选附带项。
- 提交：无（未实施）。

---

## CR009-004 正常离车闭锁请求

- 状态：CANCELLED（未实施即取消）
- 取消原因：手机走远时 BLE 超范围通常自然断连，断连闭锁已覆盖“走远闭锁”；004 只覆盖“蓝牙仍连接但人走远”的少见场景，且需要 P挡（无信号）/驾驶门开→关/稳定时间等大量标定判定，增量小、成本高。
- 提交：无（未实施）。

---

## CR009-005 断连闭锁兜底（断开前走远证据）

- 状态：BUILT（用户确认编译通过，实机待测）
- 原有逻辑：断连闭锁（CR008-010 + CR009-001）只要授权链路断开 5s + 锁状态非 LOCKED + 非静默 + 门/尾门全关就闭锁，不要求“走远证据”，蓝牙瞬时断连（手机在车旁）会误闭锁。
- 必要性：修误闭锁——手机在近区蓝牙闪断 5s 不应锁车。
- 不修改风险：不改则车旁蓝牙闪断仍会误闭锁。
- 实际改动：断连闭锁启动增加“断开前最后确认区域为远区（OUTSIDE_LOCK/PARKING_INVALID）”条件；断开前在近区（IN_CAR/UNLOCK）则不允许闭锁。
- 文件与函数：`user_app_fun/user_app_phone_peps.c` 的 `user_app_phone_peps_process()` 断连闭锁启动逻辑。
- 影响范围：只影响断连闭锁启动条件；正常走远（断开前已在远区）照常闭锁。
- 与 CR008 和 CR009 其他子项的冲突检查：收紧 CR008-010 断连闭锁条件，需回归既有通过项；与 CR009-001 门控叠加；不改冻结接口。
- 编译方法：Simplicity Studio 编译，仅改 `.c`。
- 前后对比实测方法：车旁蓝牙闪断 5s → 不闭锁（改前会）；走远后断开 5s → 正常闭锁；回归 CR008-010、CR009-001/006。
- 实际验证证据：用户确认编译通过，实机待测。
- 提交：待提交。

---

每个已实施子项按以下格式追加：

```text
## CR009-NNN 标题

- 状态：CODED / BUILT / TESTED / ACCEPTED
- 原有逻辑：
- 必要性：
- 不修改风险：
- 实际改动：
- 文件与函数：
- 影响范围：
- 与 CR008 和 CR009 其他子项的冲突检查：
- 编译方法：
- 前后对比实测方法：
- 实际验证证据：
- 提交：
```
